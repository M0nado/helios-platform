const $=id=>document.getElementById(id);document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.tab,.panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');$(b.dataset.panel).classList.add('active')}));async function post(url,data,status){$(status).textContent='Working…';const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.error||`Request failed (${r.status})`);$(status).textContent='Ready for review';return j}$('generate').addEventListener('click',async()=>{try{const j=await post('/setup/bootstrap',{tenantId:$('tenant').value,subscriptionId:$('subscription').value,resourceGroup:$('group').value,environment:$('environment').value},'setupStatus');$('script').textContent=j.script;$('digest').textContent=`SHA-256: ${j.scriptSha256} · plan only · no secrets`}catch(e){$('setupStatus').textContent=e.message}});$('copy').addEventListener('click',()=>navigator.clipboard.writeText($('script').textContent));$('propose').addEventListener('click',async()=>{try{const j=await post('/upgrades/propose',{capability:$('capability').value,reason:$('reason').value,target:'helios-control'},'upgradeStatus');$('proposal').textContent=JSON.stringify(j,null,2)}catch(e){$('upgradeStatus').textContent=e.message}});
