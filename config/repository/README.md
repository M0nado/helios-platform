# Repository Governance Configuration

This directory contains machine-readable repository topology, module-boundary and promotion-policy manifests.

Current files:

- `module-boundaries.v1.json` — canonical module ownership, dependency direction, separate-repository roles, submodule policy and merge controls.

Changes to this directory require architecture review and must preserve fail-closed merge and external-mutation controls.
