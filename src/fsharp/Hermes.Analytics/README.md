# Hermes.Analytics

F# analytics layer for deterministic math, predictions, risk scoring, and
parallel model pipelines. Keep forecasting kernels pure and testable so the
Python AI Hub bridge can orchestrate them without owning model math.
