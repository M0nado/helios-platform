const reports=['reports/final-gate/final-gate.json','reports/mass-integration/mass-integration-score.json','reports/mass-integration/conflict-forecast.json','reports/policy/policy-gate.json','reports/security/vault-readiness.json','reports/agents/runtime-matrix.json','reports/model-store/models.json','reports/github-takeover/status.json','reports/microsoft365/copilot-readiness.json','reports/hermes-xcore/models.json'];
const commands=[['Setup','./tools/helios.ps1 setup deep-all'],['GitHub takeover','./tools/helios.ps1 github takeover'],['Agent XP','./tools/helios.ps1 agents xp'],['Model store','./tools/helios.ps1 models list'],['M365 Copilot','./tools/helios.ps1 m365 copilot'],['Hermes models','./tools/helios.ps1 hermes models'],['Audit latest','./tools/helios.ps1 audit latest']];
const app=document.getElementById('app');
app.innerHTML='<section class="grid">'+commands.map(([l,c])=>`<div class="card"><h2>${l}</h2><button onclick="navigator.clipboard.writeText('${c}')">Copy: ${c}</button></div>`).join('')+reports.map(r=>`<div class="card"><h2>${r}</h2><pre>Report source: ${r}</pre></div>`).join('')+'</section>';
