# GitHub Project Setup

Create project views by `area`, `source_repo`, `risk`, and `release_gate`. External source readiness is generated by `scripts/github/prepare-consolidation.py` and should be attached to consolidation tracking issues.
