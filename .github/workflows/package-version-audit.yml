name: Package version audit

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  package-version-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify centralized NuGet package versions
        run: python3 scripts/audit-package-versions.py
