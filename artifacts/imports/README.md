# HELIOS final mainline import artifacts

The checksum records in this directory identify externally preserved, immutable consolidation packages. Binary archives are intentionally not committed through a text-only connector path. The source merge must reconstruct or apply them through the reviewed operator and verify the recorded SHA-256 values before use.
